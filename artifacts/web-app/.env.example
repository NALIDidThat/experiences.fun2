# Privy public app ID — get from https://privy.io dashboard (safe to expose)
VITE_PRIVY_APP_ID=clxxxxxxxxxxxxxxxx

# Port for the Vite dev server (default 4173 in vite.config.ts — use 5173 locally)
PORT=5173

# Port the Express API server runs on (used by Vite proxy — default 3000)
API_PORT=3000
